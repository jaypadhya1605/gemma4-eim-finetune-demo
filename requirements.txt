# Control-plane dependencies (your laptop / dev container)
# The GPU node uses a separate environment defined in training/conda.yml
# Use Python 3.10-3.12 locally. Python 3.13/3.14 may require unsupported
# native builds for Streamlit/PyArrow and Azure SDK transitive dependencies.

azure-ai-ml>=1.20.0           # AzureML v2 SDK — jobs, endpoints, data assets
azure-mgmt-resource>=23.0.0,<24.0.0  # resource group/resource deployment operations
azure-identity>=1.17.0
azure-identity-broker>=1.1.0  # lets DefaultAzureCredential use VS Code Azure sign-in
azure-keyvault-secrets>=4.8.0  # for HF token retrieval during eval
certifi>=2025.1.31            # local TLS CA bundle for Azure/HF SDK calls

# Local eval helper (eval can run remotely against endpoint OR locally if you have a GPU)
# These are only needed if you run src/05_eval.py against the deployed endpoint
httpx>=0.27.0
tenacity>=8.5.0
streamlit>=1.36.0,<1.57.0

# Utility
python-dotenv>=1.0.1
rich>=13.7.0
